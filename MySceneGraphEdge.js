/**
 * MySceneGraphEdge class, representing an edge from the scene graph.
 */
class MySceneGraphEdge {
    /**
     * @constructor Builds a scene graph node
     * @param {MySceneGraphNode} dest node to be connected to
     */
    constructor(dest) {
        this.dest = dest;
    }
}